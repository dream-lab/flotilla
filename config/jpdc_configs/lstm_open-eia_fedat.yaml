session_config:
  session_id: lstm_open-eia_fedat
  use_gpu: False
  aggregator: fedat
  aggregator_args: None
  client_selection: fedat
  client_selection_args:
    num_tiers: 3
    num_clients_selected_per_tier: 2
  checkpoint_interval: 10
  validation_round_interval: 1
  generate_plots: False


benchmark_config:
  model_id: LSTM
  model_dir: ./models/LSTM
  model_class: LSTMModel
  dataset: OpenEIA-CA
  bench_minibatch_count: 500
  batch_size: 16
  learning_rate: 0.0001
  timeout_duration_s: 180

server_training_config:
  model_dir: ./models/LSTM
  global_model_validation_batch_size: 100
  num_training_rounds: 140

client_training_config:
  model_id: LSTM
  model_class: LSTMModel
  dataset: OpenEIA-CA
  epochs: 2
  batch_size: 16
  learning_rate: 0.00001
  train_timeout_duration_s: 600
  loss_function: crossentropy
  loss_function_custom: True
  optimizer: adam
  optimizer_custom: True

model_config: # use as defaults
  use_custom_dataloader: True
  custom_loader_args:
    lookback: 5
    pred_forward: 2
    normalize_data: True

  use_custom_trainer: True
  custom_trainer_args:
    clip: 5

  use_custom_validator: True
  custom_validator_args: 
    test_data_path: /home/fedml/fedml-ng/val_data/OpenEIA-CA/test
    plot_predictions: False
    dump_metrics: True
    metrics_dir: ./LSTM_OpenEIA-CA_fedavg

  model_args:
    input_dim: 1
    hidden_dim: 128
    output_dim: 2
    layer_num: 1
    cell: LSTM
